export enum ROUTES {
	INTRO = "/",
	DASHBOARD = "/dashboard",
	GAME_ID = "/game/:id",
	GAME = "/game",
}

export enum QUERY_URL {
	GAME = "game",
}

export enum REDUCER_PATHES {
	GAME = "game",
}

export enum GAME_STATUS {
	PENDING = "pending",
	STARTED = "started",
	FINISHED = "finished",
}
