import styled from "styled-components";
import { FONT } from "styles";

export const GreetingContainer = styled.p`
  text-align: center;
  font-size: ${FONT.SIZE.LARGE};
  font-weight: 700;
`;

export const Paragraph = styled.p`
  text-align: center;
  font-size: ${FONT.SIZE.MEDIUM};
  font-weight: 600;
`;
